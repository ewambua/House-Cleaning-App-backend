databases:
  - name: postgres
    ipAllowList: []
    databaseName: House_Cleaning_App_backend
    user: House_Cleaning_App_backend

services:
  - type: web
    name: House_Cleaning_App_backend
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false